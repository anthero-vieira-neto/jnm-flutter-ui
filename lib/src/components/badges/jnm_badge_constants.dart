import 'package:jnm_ui/jnm_ui.dart';

/// Possible size values for [JNMFilledBadge] and [JNMOutlinedBadge].
enum JNMBadgeSize {
  sm,
  md,
  lg,
}

/// Possible variants for [JNMFilledBadge] and [JNMOutlinedBadge].
enum JNMBadgeVariant {
  defaultV, // "default" is a reserved keyword
  primary,
  danger,
  warning,
  success,
  info,
}
